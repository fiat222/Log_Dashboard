route:
  receiver: logstore-webhook
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: logstore-webhook
    webhook_configs:
      - url: http://backend:8000/api/alerts/webhook
        send_resolved: true
